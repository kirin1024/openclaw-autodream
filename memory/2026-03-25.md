# 2026-03-25 Daily Notes

## 小学（AI课程助教）架构 - 持续完善

### 完成的工作
1. small-school agent 配置创建完成（models.json + auth-profiles.json + SOUL.md + IDENTITY.md）
2. 小学会话已测试可通（sessions_spawn mode=run + announce）
3. 消息路由规则写入 TOOLS.md 和 MEMORY.md
4. 路由机制确认：sessions_spawn 临时唤起模式，非持续监听
5. Token 统计规则已明确：必须调用 session_status 获取真实数据，格式 `[📊 Tokens: {tokens} | 模型: {model}]`，禁止写固定 0 in / 0 out
6. Coursera TensorFlow 课程已登记到学习档案
7. 默认课程约定：陈老师不特殊说明 = AI/LLM 实战课程（自建课程）

### 关键确认
- subagent 的 mode=run 是一瞬间执行完的，发送 sessions_send 到 subagent 会话会持续超时
- subagent 架构不支持真正的"持续监听"模式
- 正确方案：每次收到课程消息 → main spawn 小学处理 → announce 结果回来 → main 转发到群 + token统计

### 小工具配置更新
- openclaw.json 已添加 tools.sessions.visibility=all
- small-school 已添加 feishu_doc 等飞书工具权限

## Token 统计教训
- 之前多次回复中错误地写了 `[📊 Tokens: 0 in / 0 out | 模型: xxx]`
- 正确做法：每次回复前调用 session_status 获取真实数据
- 已写入 MEMORY.md 重要规则

## 飞书文档更新
- ResNet 详解已写入复习笔记 AawWd71ynoU79ZxUcOIcVT5enab（补充部分）
- IVF 和 HNSW 英文全称已更新到课程文档 BG7Xdd2t8om9Q5xazvicoNdAnvc
  - IVF = Inverted File Index
  - HNSW = Hierarchical Navigable Small World
  - ANN = Approximate Nearest Neighbor
  - KNN = K-Nearest Neighbors
- ResNet 代码示例已插入到课程文档的向量检索和电商匹配代码块中
